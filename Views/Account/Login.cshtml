@using System.Collections.Generic
@using System.Linq
@using Microsoft.AspNetCore.Http

@model LoginViewModel
@inject SignInManager<ApplicationUser> SignInManager

@{
    ViewData["Title"] = "Log in";
}
<style>
    body {
        background-image: url(/images/colors.jpg);
        background-repeat: no-repeat;
        height: 100%;
        width: 100%;
        background-size: cover;
    }

</style>

<div class=" container ">
    <div class="col-md-4">
    </div>

    <div class="col-md-4 container-fluid glyphicon-bold" style="background-color:white; margin-top:5%">
        <h2 class="text-center" style="margin-top:-13%; color:white">Mtama</h2>
        <br />
        <h4 class="text-center"> User Login</h4>
        <form asp-route-returnUrl="@ViewData["ReturnUrl"]" method="post">
            <h6 class="text-center">Please enter your Cell No. and Password.</h6>
            <hr />
            <div asp-validation-summary="All" class="text-danger"></div>
            @*<div class="form-group">
            <input asp-for="NationalId" placeholder="National ID" class="form-control" />
            <span asp-validation-for="NationalId" class="text-danger"></span>
        </div>*@
            <div class="form-group">
                <input asp-for="PhoneNumber" type="text" placeholder="07xx xxx xxx"  value="" class="form-control" />
                <span asp-validation-for="PhoneNumber" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input asp-for="Password" placeholder="Password" class="form-control" value="" />
                <span asp-validation-for="Password" class="text-danger"></span>
            </div>
            <div class="form-group">
                <p>
                    <a class="btn-link" asp-action="ForgotPassword">Forgot password?</a>
                </p>
            </div>

            @*<div class="form-group">
            <div class="checkbox">
                <label asp-for="RememberMe">
                    <input asp-for="RememberMe" />
                    @Html.DisplayNameFor(m => m.RememberMe)
                </label>
            </div>
        </div>*@
            <br />
            <div class="form-group">
                <button type="submit" class="btn btn-default form-control text-center" style="background-color:darkorange; color:white"> Login</button>
            </div>
            <br />
            @*<div class="form-group">
                <p>
                    New Member?
                    <a asp-action="Register" class="btn-link" asp-route-returnurl="@ViewData["ReturnUrl"]"> Register</a>
                </p>
            </div>*@
        </form>
        <br />
        <br />
    </div>
    @*<div class="col-md-6 col-md-offset-2">
            <section>
                <h4>Use another service to log in.</h4>
                <hr />
                @{
                    var loginProviders = (await SignInManager.GetExternalAuthenticationSchemesAsync()).ToList();
                    if (loginProviders.Count == 0)
                    {
                        <div>
                            <p>
                                There are no external authentication services configured. See <a href="https://go.microsoft.com/fwlink/?LinkID=532715">this article</a>
                                for details on setting up this ASP.NET application to support logging in via external services.
                            </p>
                        </div>
                    }
                    else
                    {
                        <form asp-action="ExternalLogin" asp-route-returnurl="@ViewData["ReturnUrl"]" method="post" class="form-horizontal">
                            <div>
                                <p>
                                    @foreach (var provider in loginProviders)
                                    {
                                        <button type="submit" class="btn btn-default" name="provider" value="@provider.Name" title="Log in using your @provider.DisplayName account">@provider.Name</button>
                                    }
                                </p>
                            </div>
                        </form>
                    }
                }
            </section>
        </div>*@
</div>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}
